এই zip আগের "AI মডেল সিলেক্টর" zip-এর ওপরে বসে (সেটা আগে বসানো থাকতে হবে)। এখানে
শুধু "🔍 টেস্ট করুন" বাটন যোগ করা হয়েছে — key+মডেল সত্যিই কাজ করছে কিনা গেস না করে
সরাসরি যাচাই করার জন্য।

EDITED (৩টা ফাইল):
  app/src/main/java/com/hanif/smartstudy/data/remote/WrittenAnswerAiService.kt
  app/src/main/java/com/hanif/smartstudy/viewmodel/MenuViewModel.kt
  app/src/main/java/com/hanif/smartstudy/ui/menu/SettingsScreen.kt

কী যোগ হলো:
  - Settings-এ প্রতিটা মডেল-ড্রপডাউনের পাশে একটা "🔍 টেস্ট" বাটন।
  - চাপলেই (এখনো "সংরক্ষণ করুন" না চাপলেও) সেই key+মডেল দিয়ে একটা ছোট্ট আসল
    রিকোয়েস্ট পাঠায়, আর সাথে সাথে স্পষ্ট বাংলা বার্তা দেখায়:
      ✅ কাজ করছে
      ❌ API key ভুল/অবৈধ
      ❌ এই মডেল খুঁজে পাওয়া যায়নি — নাম ভুল বা বন্ধ হয়ে গেছে
      ⚠️ Rate limit — key/মডেল ঠিক আছে, একটু পর আবার চেষ্টা করুন
      ❌ নেটওয়ার্ক এরর
  - এতে ৪টা মডেল একে একে টেস্ট করে সহজেই বের করা যাবে কোনটা এখন আসলেই কাজ করছে,
    গেস/অনুমান করার দরকার নেই।

পুশ করার আগে:
  ১. উপরের ৩টা ফাইল রিপোতে replace করুন
  ২. build/run করে Settings-এ গিয়ে যেকোনো একটা "🔍 টেস্ট" বাটনে চেপে দেখুন বার্তা
     আসছে কিনা
  ৩. সব ঠিক থাকলে push করুন
