এই zip-এ শুধু এই ফিচারের (পুরনো AI বাটন রিমুভ + নতুন "AI ব্যাখ্যা" বাটন) জন্য বদলানো/নতুন
ফাইলগুলোই আছে। এটা আগের "Data Source ফিচার রিমুভ" zip-এর ওপরেই বসবে — ওই ফাইলগুলো (৭টা
এডিটেড + README_FIRST.md) যদি এখনো আপনার রিপোতে না বসিয়ে থাকেন, আগে সেটা করুন, তারপর এটা।

EDITED (৪টা ফাইল):
  app/src/main/java/com/hanif/smartstudy/data/remote/WrittenAnswerAiService.kt
  app/src/main/java/com/hanif/smartstudy/viewmodel/QuizViewModel.kt
  app/src/main/java/com/hanif/smartstudy/ui/shared/SharedComponents.kt
  app/src/main/java/com/hanif/smartstudy/ui/quiz/QuestionListScreen.kt

NEW (১টা ফাইল):
  app/src/main/java/com/hanif/smartstudy/util/AiExplanationCache.kt

কী বদলেছে (সংক্ষেপে):
  - প্রতিটা প্রশ্নের হেডারে থাকা পুরনো "AI" বাটন (ভয়েস-চ্যাট খুলত) সরানো হয়েছে।
  - উত্তর সাবমিট করার পর নতুন "🤖 AI ব্যাখ্যা" বাটন দেখা যাবে — ক্লিক করলে প্রশ্নের পূর্ণ
    ব্যাখ্যা আনবে (অঙ্ক হলে ধাপে ধাপে সমাধান, ইংরেজি গ্রামার হলে বাংলা-ইংরেজি মিশিয়ে),
    আপনার existing key rotation দিয়েই (Groq→Mistral→Cerebras→Gemini)।
  - ব্যাখ্যা শুধু ইউজারের ফোনে (local JSON file) ক্যাশ হয় — কোনো ডাটাবেস/সার্ভারে না।

⚠️ এখন আর ব্যবহৃত হচ্ছে না (dead code, কিন্তু ইচ্ছাকৃতভাবে ডিলিট করা হয়নি):
  app/src/main/java/com/hanif/smartstudy/ui/aichat/QuestionVoiceAiSheet.kt
  app/src/main/java/com/hanif/smartstudy/viewmodel/QuestionVoiceAiViewModel.kt
  → এই দুটো ফাইল আগের প্রশ্ন-ভিত্তিক ভয়েস-AI-চ্যাট ফিচারের অংশ ছিল, যেটার বাটন এখন
    সরানো হয়েছে। ফাইলদুটো এখনো রিপোতে আছে (কোনো ক্ষতি করছে না, শুধু অব্যবহৃত), চাইলে
    ভবিষ্যতে আলাদাভাবে ডিলিট করাতে পারেন — এই zip-এ সেটা করা হয়নি কারণ আপনি শুধু
    "বাটন রিমুভ" বলেছিলেন, পুরো ফাইল/ফিচার ডিলিটের কথা বলেননি।

পুশ করার আগে:
  ১. উপরের ৪টা ফাইল আপনার রিপোতে replace করুন
  ২. AiExplanationCache.kt নতুন ফাইলটা ঠিক জায়গায় (util প্যাকেজে) যোগ করুন
  ৩. Android Studio-তে build/run করে দেখুন — বিশেষভাবে: (ক) কোনো প্রশ্নে উত্তর দেওয়ার
     পর "AI ব্যাখ্যা" বাটন দেখা যাচ্ছে কিনা, (খ) ক্লিক করলে ব্যাখ্যা আসছে কিনা (Settings-এ
     অন্তত একটা AI key সেভ করা থাকতে হবে টেস্ট করতে), (গ) প্রশ্নের হেডারে পুরনো "AI"
     বাটনটা আর নেই কিনা
  ৪. সব ঠিক থাকলে push করুন
